{
  "mcpServers": {
    "github": {
      "command": "npx",
      "args": [
        "-y",
        "@github/github-mcp-server"
      ]
    },
    "atlassian": {
      "command": "npx",
      "args": [
        "-y",
        "@sooperset/mcp-atlassian",
        "--baseUrl",
        "https://your-company.atlassian.net"
      ],
      "env": {
        "ATLASSIAN_USERNAME": "${COMPANY_ATLASSIAN_USERNAME}",
        "ATLASSIAN_API_TOKEN": "${COMPANY_ATLASSIAN_API_TOKEN}"
      }
    }
  }
}
