{
  "mcpServers": {
    "test": {
      "command": "test-mcp-server",
      "args": [],
      "timeout": 5000
    },
    "github": {
      "command": "github-mcp-server",
      "args": ["stdio"],
      "env": {
        "GITHUB_PERSONAL_ACCESS_TOKEN": "${GITHUB_TOKEN}"
      },
      "timeout": 5000
    }
  }
}
