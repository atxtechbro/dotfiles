# Modular tmux configuration
# Sources separate files for different concerns

# Load general settings
source-file ~/ppv/pillars/dotfiles/.tmux/settings.conf

# Load key bindings
source-file ~/ppv/pillars/dotfiles/.tmux/bindings.conf

# Load styling
source-file ~/ppv/pillars/dotfiles/.tmux/style.conf
